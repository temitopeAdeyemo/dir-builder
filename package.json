{
  "name": "yaml-dir-builder",
  "version": "1.1.0",
  "description": "Library for building file system from yaml file",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "bin": {
    "make-yaml-dir": "./dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.5.0"
  },
  "author": "Temitope Adeyemo",
  "license": "MIT"
}
